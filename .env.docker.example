# FairFlow Docker Environment Configuration
# Copy this to .env and fill in your values

# Database Configuration
# IMPORTANT: You must provide your own managed PostgreSQL database
# Supported providers: Neon, AWS RDS, Google Cloud SQL, Azure Database, etc.
#
# Example DATABASE_URL formats:
# - Neon: postgresql://user:password@ep-xxx.region.aws.neon.tech/fairflow?sslmode=require
# - AWS RDS: postgresql://user:password@your-instance.region.rds.amazonaws.com:5432/fairflow?sslmode=require
# - Cloud SQL: postgresql://user:password@host/fairflow?sslmode=require
DATABASE_URL=postgresql://user:password@your-managed-db-host:5432/fairflow?sslmode=require

# Security
JWT_SECRET=your_random_32_character_secret_key_here_minimum_32_chars

# Server Configuration
PORT=3000
ENVIRONMENT=production
LOG_LEVEL=info

# Notes:
# - Never commit the actual .env file to git
# - Set up your managed PostgreSQL database before running docker-compose
# - Use strong passwords in production
# - JWT_SECRET should be at least 32 characters
# - Always use sslmode=require for production databases
